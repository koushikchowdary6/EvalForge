[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Markers for test categorization
markers =
    api: tests that require API access (Anthropic/OpenAI)
    unit: unit tests
    integration: integration tests
